import React from 'react';

type Props = {

};

const Navigator = (props: Props) => {
    const {} = props;
    return (
        <>
            Navigator
        </>
    );
}

export default Navigator;